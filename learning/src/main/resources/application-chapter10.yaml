spring:
  application:
    name: learning
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: chapter10-default-group
      auto-offset-reset: earliest
    producer:
      acks: all